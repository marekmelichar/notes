#!/bin/sh

REPO_ROOT="$(git rev-parse --show-toplevel)"

echo "Validating docs..."
node "$REPO_ROOT/scripts/validate-docs.mjs" || exit 1

cd "$REPO_ROOT/ui"

echo "Running lint..."
npm run lint || exit 1

echo "Running build..."
npm run build || exit 1

echo "Pre-commit checks passed!"
