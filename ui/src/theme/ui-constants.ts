/**
 * UI Constants
 *
 * Centralized constants for consistent spacing, sizing, and styling across the application.
 * These values should be used instead of magic numbers/strings in components.
 */

// Map Marker Sizes
export const MAP_MARKER = {
  SIZE: 22, // Default marker size in pixels
} as const;

// Layout constants
export const LAYOUT = {
  HEADER_HEIGHT: 64, // 4rem
  MOBILE_NAV_HEIGHT: 56,
  MOBILE_BREAKPOINT: 768,
} as const;
